/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppMovie from './src/AppMovieVer1';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppMovie);
