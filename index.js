/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppMovie from './srcAppMovie/AppMovie';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppMovie);
